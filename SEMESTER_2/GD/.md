

#           # Simulation Commands :On the rogue device (attacker)
```json
{
            sudo arpspoof -i eth0 -t 192.168.30.10 192.168.30.1     # Poison victim (PC) gateway
            sudo arpspoof -i eth0 -t 192.168.30.1 192.168.30.10     # Poison gateway
}
```

#         # Basic Capture on Switch or Server: Capture ARP traffic
`          sudo tcpdump -i eth0 arp -nn -c 50                                                                 `

#         # Example suspicious output (ARP Poisoning):
```json
{                                                          
 //         14:22:15.123456 ARP, Request who-has 192.168.30.1 tell 192.168.30.50                                
**          14:22:15.124123 ARP, Reply 192.168.30.1 is-at 00:11:22:33:44:55  # Fake MAC from rogue device
}
```

#         # Advanced Capture for Investigation
```json
{                                                          
 `         sudo tcpdump -i eth0 -nn -vv -c 100 "arp or icmp or port 445" -w incident.pcap
}
```

#         # Recommended Detection & Response Commands
```json
{                                                          
         # Check ARP table for anomalies
//         arp -a

         # On Cisco/Ubiquiti switch (detection)
//         show arp | show mac address-table

         # Enable dynamic ARP inspection (DAI) prevention
//         ip arp inspection vlan 30

}
```
